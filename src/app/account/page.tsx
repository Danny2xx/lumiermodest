import ContentPage from "@/components/ContentPage";

export const metadata = { title: "Account — LumierModest" };

export default function AccountPage() {
  return (
    <ContentPage title="Account">
      <p>
        Customer accounts — order history, saved addresses, and sign-in —
        aren&apos;t connected yet. This is a placeholder page reserved for
        that once authentication is wired up.
      </p>
    </ContentPage>
  );
}
